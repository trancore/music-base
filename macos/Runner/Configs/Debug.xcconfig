#include "../../Flutter/Flutter-Debug.xcconfig"
#include "Warnings.xcconfig"

// App Store signing is intentionally outside the current scope. CI and local
// validation use an unsigned desktop bundle.
CODE_SIGNING_ALLOWED = NO
