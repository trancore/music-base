# Development setup

Prepare the Flutter SDK, Windows desktop tooling, Android tooling, and Node.js.

```text
flutter pub get
pnpm install
flutter analyze
flutter test
pnpm docs:build
```

Start the documentation development server with `pnpm docs:dev`.
