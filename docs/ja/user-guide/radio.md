# インターネットラジオ

「Radio」画面から、インターネットラジオ局を登録して再生できます。局名と音声ストリームURLを入力し、「Test and save」を実行してください。

虫眼鏡のアイコンからRadio Browserを検索できます。検索結果はRadio Browserが到達可能と判定した局に絞り、プレイリストやリダイレクトを解決した直接ストリームURLを使用します。局を選択すると先に保存し、そのまま再生を開始します。一時的なネットワーク障害や配信停止で再生を開始できない場合はエラーを表示しますが、登録は保持されるため、後から再試行できます。応答しない配信先への接続は12秒で打ち切ります。

音声ストリームURLは、ラジオのWebページではなく、音声データを直接配信するURLです。たとえば、HappyHardcore.comのページURLは次のURLです。

```text
https://www.happyhardcore.com/radio/player/radio.asp
```

これは登録できません。ページ内で使用されている直接ストリームURLの例は次のとおりです。

```text
https://u1.happyhardcore.com/
```

登録済みの局は再生、編集、削除ができます。ストリームURLは配信元の都合で変更されることがあるため、再生できなくなった場合は配信元で最新URLを確認して編集してください。局の情報はこの端末の設定へ保存されます。

画面上部のインポート／エクスポートから、局名、ストリームURL、説明、ジャンルをJSONファイルで移行できます。インポート時に既存のIDまたはストリームURLと重複する局は追加されません。

局一覧は「Manual order」「Name (A–Z)」「Name (Z–A)」「Genre」から並び順を選べます。選択した並び順はこの端末に保存され、次回も復元されます。「Genre」では先頭のジャンルタグごとに局をグループ表示し、ジャンル未設定の局は「Other」にまとめます。Manual orderでは、局左側のハンドルをドラッグして順番を変更できます。手動順もこの端末に保存されます。
