# AGENTS.md

## プロジェクト概要

このリポジトリは、WindowsとAndroid向けのFlutter製ミュージックプレイヤーです。
最初のリリース対象はWindowsとし、Windows版が安定した後にAndroid対応を進めます。

音源参照先は、ローカルディレクトリまたはSMB共有から選択できるようにします。ユーザーが選択した参照先を変更・保存できるようにします。WindowsではCDリッピングを提供し、Androidでは提供しません。リッピング形式はFLACまたはMP3からユーザーが選択できます。アルバム情報はMusicBrainzから取得します。プレイヤーにはテーマ、ビジュアライザ、リアルタイムスペクトルアナライザを実装します。

このリポジトリには、`docs/` 配下に日本語・英語のVitePressドキュメントサイトも含めます。ドキュメントは利用者向けと開発者向けの両方を対象とし、GitHub Pagesへ公開します。

## 作業ルール

- 編集前に、関連する実装、テスト、ドキュメントを確認する。
- 大規模な機能追加やアーキテクチャ変更では、最初に簡潔な実装計画を提示する。
- UI、状態管理、ドメインロジック、データアクセス、プラットフォーム固有処理を分離する。
- SMBアクセス、音楽再生、MusicBrainz連携、タグ処理、CDリッピング、音声解析を独立したサービス境界として扱う。
- Windows専用のCD処理やネイティブ連携コードを、Androidと共有するコードへ直接混在させない。
- 選択されたローカルディレクトリまたはSMB共有を音源の正本として扱い、ローカルDBはキャッシュとアプリ内状態として扱う。
- SMBの認証情報やその他の秘密情報を、ログ、コミット、ソースコードへ記録しない。
- ユーザーの明示的な操作と、必要に応じた確認なしに、音源参照先のファイルを削除、移動、上書きしない。
- ネットワーク障害、認証失敗、ファイル消失、ドライブ未接続を、復旧可能な通常のエラー状態として扱う。
- 本番依存パッケージを追加する前に、Windows・Android対応、ライセンス、保守状況、既存依存パッケージとの重複を確認する。
- 外部APIやパッケージの現在の仕様を、公式ドキュメントで確認する。
- 無関係なリファクタリングを避け、変更範囲を依頼内容に限定する。

## 音源・MusicBrainzルール

- 音源参照先はローカルディレクトリまたはSMB共有から選択でき、設定した参照先は保存・編集可能にする。
- MusicBrainzへのリクエストには識別可能なUser-Agentを設定し、サービスの利用制限を守り、必要に応じてキャッシュする。
- 外部APIのレスポンスをそのままUIへ渡さず、アプリ内モデルへ変換する。
- メタデータ取得に失敗した場合も、手動編集または再試行によって取り込みを継続できるようにする。
- 音声解析が利用できない場合でも、通常の再生を失敗させない。
- 音声解析の更新頻度とリソース使用量に上限を設ける。

## ドキュメントルール

- 利用者から見える挙動を変更した場合、日本語と英語の両方のドキュメントを更新する。
- 公開API、データモデル、アーキテクチャ、コマンド、セットアップを変更した場合、開発者向けドキュメントを更新する。
- 日本語と英語で情報構成を揃える。
- リポジトリに存在しないコマンド、パス、機能をドキュメントへ記載しない。
- 長期的な影響を持つアーキテクチャ決定は、`docs/` 配下に短い意思決定記録を追加する。

## 検証

変更内容に関連する検証を実行し、実行できなかったものと理由を報告する。

```text
dart format .
flutter analyze
flutter test
flutter build windows
pnpm docs:build
```

Android対応後は、必要に応じて `flutter build apk` も実行する。
ドキュメントのUIを変更した場合は、`pnpm docs:preview` を実行して表示を確認する。

## ドキュメントサイト

- VitePressのソースは `docs/` 配下に置く。
- 日本語ページは `docs/ja/`、英語ページは `docs/en/` に置く。
- 利用者向けガイドと開発者向けガイドを分ける。
- `docs/.vitepress/cache` と `docs/.vitepress/dist` をバージョン管理へ追加しない。
- GitHub Pagesのワークフローは、公開前に必ずドキュメントをビルドする。
